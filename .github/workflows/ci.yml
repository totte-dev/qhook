name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2

      - name: Security audit
        run: cargo install cargo-audit --locked && cargo audit --ignore RUSTSEC-2023-0071 --ignore RUSTSEC-2026-0009

      - name: Doc sync check
        run: bash scripts/check-docs.sh

      - name: Format
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets

      - name: Unit tests
        run: cargo test

      - name: Build release
        run: cargo build --release

      - name: E2E tests
        run: bash tests/e2e.sh
