apiVersion: v1
kind: Service
metadata:
  name: {{ include "qhook.fullname" . }}
  labels:
    {{- include "qhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qhook.selectorLabels" . | nindent 4 }}
